# 更新履歴

MidFD の主な変更点をまとめます。
詳細な開発ログではなく、利用者向けの変更概要です。

## Unreleased

## v2026.05.24

### 追加
- 一覧表示モード（ファイル名のみ / サイズ / サイズ+更新日時）を追加。
- 表示色プリセットとユーザー配色プリセットの導線を追加。
- Logdsk / Move 先入力欄の履歴呼び出し（Up/Down）を追加。
- FD/WinFD互換ショートカットを拡充。
- アンダーラインカーソル表示を追加。

### 改善
- Explorer 起動導線を Alt+F2 / メニューから利用可能にした。
- Logdsk / Move 先入力欄で、履歴選択（Up/Down）と補完（Tab）の責務を分離。
- 7-Zip 外部プロセス実行時の stdout/stderr drain を補強し、エラー出力が多い場合の固まりリスクを低減。
- 7-Zip 選択解凍を `@listfile + -scsUTF-8` 方式に変更し、大量選択・日本語/空白名の安定性を改善。
- symlink / junction / reparse point を含む削除・移動で、リンク先実体へ再帰しない安全境界を追加。
- 複数項目の切り取り貼り付け（移動）で実行前確認を追加し、成功時はセッション内 Undo / Redo 可能にした。

### 仕様
- reparse point directory のコピーは、リンク先展開コピーを避けるため安全側でブロック。
- reparse point directory の別ドライブ move fallback は、危険な copy+delete を避けるため安全側でブロック。

## v2026.05.20

### 追加
- 初回起動時の「初回セットアップ」画面を追加。
  - 利用モードを選択できます。
  - FD互換ファンクションキー配置を選択できます。
  - 動画ファイルの Enter 動作を選択できます。
  - 7-Zip、動画ツールフォルダ、外部エディタを任意で設定できます。
- 設定画面の「起動 / 復元」から、初回セットアップを再表示できるようにしました。
- 動画ファイルの Enter 動作を外部再生に切り替える設定を追加しました。

### 変更
- 動画ツール設定を、`ffmpeg.exe` 個別指定から「動画ツールフォルダ」指定へ変更しました。
- `ffmpeg.exe`、`ffplay.exe`、`ffprobe.exe` を役割別に自動検出するようにしました。
- 動画の外部再生は、`ffplay.exe` が未設定の場合でも Windows の関連付けで開くようにしました。
- ffmpeg 未設定時の動画プレビュー導線は、黒い空画面ではなく Binary Viewer へフォールバックするようにしました。

### 修正
- 動画静止画プレビューが黒画面になることがある問題を修正しました。
- 初回セットアップ画面の文字あふれとボタン配置を調整しました。
- Browser 上での Ctrl+Enter による動画外部再生導線を整理しました。

### 注意
- 動画静止画プレビューには `ffmpeg.exe` が必要です。
- 動画の外部再生は、`ffplay.exe` がなくても Windows の関連付けで開けます。
- 初回セットアップの再表示は、全設定の初期化ではありません。

## v2026.05.17

### 初回公開
- MidFD の初回公開版です。
- キーボード中心のファイル操作、タブ、カテゴリ、マーク、基本プレビュー、7-Zip 連携、外部ツール連携などを含みます。
