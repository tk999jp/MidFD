# MidFD v2026.05.24.1

MidFD v2026.05.24.1 は、v2026.05.20 以降の改善をまとめた更新版です。

ファイル一覧の表示モード、配色設定、FD/WinFD互換操作、Archive Contents、7-Zip連携、ファイル操作の安全性、Text Viewer、VideoStill などを改善しました。

また、v2026.05.24.1 では配布ZIPに README / CHANGELOG / LICENSE / UserDocs を同梱し、ZIP単体でも説明書を参照できるようにしました。

## 主な追加

### ファイル一覧表示

- 一覧表示モードを追加しました。
  - ファイル名のみ
  - ファイル名 + サイズ
  - ファイル名 + サイズ + 更新日時
- 通常モードでは Ctrl+1 / Ctrl+2 / Ctrl+3 で表示モードを切り替えられます。
- FD/WinFD互換モードでは、同じ列数キーの再押下で表示モードをローテーションできます。

### 配色 / カラーモード

- 表示色プリセットとユーザー配色プリセットの導線を整理しました。
- 一覧配色をユーザー側で登録・上書き・削除できるようにしました。
- built-in 配色を直接編集した場合は、user preset として保存されます。
- user preset 編集時は、そのプリセットを上書き保存できます。
- UI基調色の扱いを整理しました。
  - 通常は表示色に連動します。
  - 必要な場合は、ファイラー背景/文字、ビューア背景/文字を手動指定できます。
- Viewer / Text Viewer / Binary Viewer の既定色を、MidFDらしい濃紺基調へ調整しました。

### FD/WinFD互換操作

- FD/WinFD互換ショートカットを拡充しました。
- FD/WinFD互換モード時に、Shift+F 系ショートカットを既存機能へ安全に接続しました。
- Shift+Enter による外部エディタ起動、Alt+F5 による設定画面表示、Shift+Ctrl+C によるフルパスコピーなどを追加しました。
- 通常モードでは FD/WinFD互換 alias が動作しないようにし、モードごとの操作境界を明確にしました。

### Explorer / Shell 起動

- Explorer 起動導線を追加しました。
  - Alt+F2
  - メニューからの起動
- FD/WinFD互換モード時に、Shift+F6 で Shell を起動できる導線を追加しました。

### アンダーラインカーソル

- 一覧カーソルを背景ハイライトではなく、下線で表示するオプションを追加しました。
- アンダーラインカーソル有効時は、ファイル種別色や属性色を保ったまま現在位置を示せます。

### Logdsk / Move 先履歴

- Logdsk と Move 先入力欄に履歴呼び出しを追加しました。
- Up / Down で履歴を選択できます。
- Tab 補完と履歴選択の責務を分離し、補完候補と履歴が混ざらないようにしました。
- 履歴は移動成功時のみ保存されます。

## Archive / 7-Zip まわりの改善

### Archive Contents

- Archive Contents 画面の操作性を改善しました。
- Enter で ZIP 内テキストファイルを独立プレビューできます。
- プレビュー表示中も Archive Contents 側のカーソル移動を続けられます。
- カーソル移動に合わせて、プレビュー内容も追従します。
- U キーでマーク済みファイル、または現在選択中のファイルを解凍できます。
- マークがない状態で実フォルダを選択して U キーを押した場合、そのフォルダ配下の実ファイルを解凍対象にします。
- `..` や空フォルダは安全に警告して停止します。
- ReadOnly タブでは解凍操作をブロックします。

### ZIP fallback

- 7-Zip 未検出環境でも、ZIP fallback による選択解凍に対応しました。
- ルート階層、サブフォルダ内、複数階層またぎのマーク解凍でも、選択した entry のみを展開できます。
- path traversal guard を追加し、解凍先ディレクトリ外への展開を防ぐようにしました。

### 7-Zip連携の安定化

- 7-Zip 外部プロセスの stdout / stderr drain を補強しました。
- エラー出力が多い場合の固まりリスクを低減しました。
- 7-Zip 選択解凍を `@listfile + -scsUTF-8` 方式に変更しました。
- 大量選択、日本語ファイル名、空白を含むファイル名の安定性を改善しました。

## ファイル操作の安全性改善

### reparse point / junction / symlink 対応

- symlink / junction / reparse point を含む削除・移動で、リンク先実体へ意図せず再帰しない安全境界を追加しました。
- reparse point directory のコピーは、リンク先展開コピーを避けるため安全側でブロックします。
- reparse point directory の別ドライブ move fallback は、危険な copy+delete を避けるため安全側でブロックします。
- 通常フォルダの削除・コピー・移動には回帰がないよう調整しています。

### 複数 cut-paste move の安全ガード

- 複数項目の切り取り貼り付け移動で、実行前確認を追加しました。
- 確認ダイアログでは、移動件数、移動元、移動先、Undo可能範囲を表示します。
- cut-paste move が全件成功した場合のみ、セッション内 Undo / Redo 履歴を記録します。
- 同名衝突、スキップ、失敗、キャンセル、統合経路では Undo 記録しない安全仕様です。
- Undo / Redo は今回、切り取り貼り付け移動に限定しています。
- コピー、削除、リネーム、アーカイブ解凍は Undo / Redo 対象外です。

## Text Viewer / LargeText Viewer

- Text Viewer / LargeText Viewer の改行認識を改善しました。
- LF / CR / CRLF / 混在改行を含むテキストでも、1行扱いになりにくくなりました。
- Markdown やログファイルなど、改行コードが混在するファイルの表示が安定しました。
- この補正は表示専用で、元ファイルの保存内容や改行コード設定には影響しません。

## VideoStill / 動画プレビュー

- VideoStill の 600秒固定上限を撤去しました。
- ffprobe が使える環境では、動画長に応じた範囲で位置指定できます。
- ffprobe が無効、または動画長が取得できない場合は、位置バーを無効化します。
- 動画長不明時に指定秒が実動画長を超える場合は、静止画を取得できないことがあります。
- 動画静止画プレビューには `ffmpeg.exe` が必要です。
- 動画外部再生には `ffplay.exe` を使用できます。未設定の場合は Windows の関連付けで開きます。

## ドキュメント / 配布物

- 配布ZIPに以下を同梱しました。
  - `README.md`
  - `CHANGELOG.md`
  - `LICENSE`
  - `UserDocs/`
- ZIP単体でも、ユーザーガイドやキーバインド説明を参照できます。

## 配布物

- `MidFD-win-x64.zip`
- `MidFD-win-x64.zip.sha256`

SHA256:

```text
34405e133dcc2e61510fa2e299d11895b1bd66dd0cc2a3b08e6a8bf642e89f20
````

## 注意

* このZIPは .NET Runtime 非同梱の framework-dependent 版です。
* 実行には .NET 8 Windows Desktop Runtime が必要です。
* 動画静止画プレビューには `ffmpeg.exe` が必要です。
* 動画の外部再生には `ffplay.exe` を使用できます。未設定の場合は Windows の関連付けで開きます。
* reparse point directory のコピーと別ドライブ move fallback は、安全のためブロックされる場合があります。
