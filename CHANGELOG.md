# 更新履歴

MidFD の主な変更点をまとめます。
詳細な開発ログではなく、利用者向けの変更概要です。

## v2026.07.04 - .NET 10移行とQuickAccess/メディア再生改善

> 公開候補版です。公開日は GitHub Release 作成時に確定します。

### 配布・実行環境
- .NET 10 を公開ビルド対象にします。
- 通常ZIP（Framework-dependent）を基本配布とし、リリース ZIP root に Desktop Runtime 導入導線として `README_FIRST.txt` を同梱します。
- GitHub の `Code > Download ZIP` / `Source code (zip)` はソースコード用であり、通常利用向けの実行パッケージではありません。
- `README_FIRST.txt` では `.NET 10.0 Desktop Runtime` を案内し、7-Zip / ffmpeg / ffprobe / ffplay は同梱しない前提を明示します。
- Portable / Installed 用のローカルパッケージ生成と storage profile diagnostics を整備しました。公開 asset として採用する範囲は release 前に確定します。

### QuickAccess / お気に入り
- QuickAccess の登録先一覧で、カテゴリ列・カテゴリフィルタ・カテゴリ見出し表示を追加しました。
- `未分類` を最後にまとめ、カテゴリの開閉、カテゴリ自体の並べ替え、カテゴリ内の項目並べ替えに対応しました。
- QuickAccess 起動後、一覧フォーカス中に `1`〜`9` で表示中候補へ直接移動できるようにしました。
- 検索欄では `1`、`7z`、`2026` など数字を含む検索語を通常入力できるようにし、数字ショートカットとの衝突を避けました。
- `Ctrl+F` または `/` で検索欄へ移動できるようにし、`Tab` / `Shift+Tab` の導線を整理しました。
- お気に入りメニュー側もカテゴリ順を反映し、QuickAccess 管理画面と見え方を揃えました。

### 設定・保存先
- アプリ設定の正本を SQLite (`Data\Settings\settings.db`) へ移行しました。
- 従来の `settings.json` は初回 import / backup 用として扱い、通常保存では再生成しない構成にしました。
- Portable 既定を維持しつつ、Installed profile の明示 opt-in と `%LOCALAPPDATA%\MidFD` 配下の設定 DB 導線を追加しました。
- Managed Trash / Workspace / Session Restore / QuickAccess の実移行は行わず、診断・将来候補として分離しています。

### 操作・検索
- Command Palette を拡張し、機能名・説明・設定を検索して実行しやすくしました。
- ファイル操作系コマンドは確認導線を挟む形に整理し、誤実行しにくくしました。

### ファイル操作・ドラッグ
- MidFD から Explorer へのドラッグ時の Copy / Move 判定を Explorer 互換に近づけました。
- Drag ZIP のドロップ互換性を改善し、ChatGPT などへの添付操作で扱いやすくしました。
- Explorer 右ドラッグの完全な Shell 互換（例: 「ショートカットをここに作成」）は未対応です。

### プレビュー
- Large text preview で通常選択、Shift+click 範囲選択、Ctrl+A、Ctrl+C の挙動を改善しました。
- Markdown / SQLite の read-only preview を追加し、編集用途は MidEditor 連携を優先する方針にしました。
- メディアファイルの Enter 外部再生を整理し、動画は設定で切り替え、音声は設定に関係なく外部再生するようにしました。

### タブ・表示設定
- タブ文字サイズとタブ幅を設定できるようにしました。
- 一覧表示フォントサイズ、タブ文字サイズ、タブ幅を個別に初期値へ戻せるようにしました。
- タブ幅を小さくしてもタブ表示が消えないようにしました。
- ステータス表示などの表示色カスタマイズ範囲を拡張し、状態表示の視認性を調整しやすくしました。

## v2026.06.14 (2026-06-14)

### 追加・改善
- カテゴリをドラッグで並べ替えできるようにした。
- タブ一覧メニューをカテゴリ別に表示し、目的のタブを選びやすくした。
- カテゴリ追加ボタンの表示を整理した。
- ファイル一覧の表示色プリセットと個別カスタマイズの使い分けを整理した。

### 不具合修正
- 一部のドライブルートなどで、タブ名に別の QuickAccess 名が誤って表示される問題を修正した。
- フォルダ移動後に、メニュー項目の有効/無効表示が古い状態のまま残ることがある問題を修正した。
- カテゴリやタブを切り替えた後、右クリック対象やタブ選択状態が前の状態を引きずることがある問題を修正した。
- MidFD内部のごみ箱管理で、古い退避データの整理が正しく行われない場合がある問題を修正した。
- コピーや移動の進行ダイアログで、表示位置や件数表示が分かりづらくなる問題を修正した。
- Drag ZIP 利用後の一時ファイル整理が正しく行われない場合がある問題を修正した。

## v2026.06.07 (2026-06-07)

### 設定・操作プロファイル
- 操作プロファイルの変更UIを「入力割り当て」タブへ一本化し、「操作」タブ側の重複コンボを撤去。
- 「操作」タブのレイアウトを整理し、「ファイル操作」「Drag ZIP」「詳細オプション」のみの構成にした。
- 「入力割り当て」のプロファイル切り替え時に、機能別・ファンクションキー・マウスジェスチャーの各表示が即座に同期されるよう改善した。
- ファンクションキーおよび通常キーの表示名、CommandId、通常キー、hotkey強調の不整合を解消した。

### 操作カスタマイズ
- Browser 画面のキーバインドを Command 単位で変更できるようにした。
- Browser 画面の右ドラッグマウスジェスチャーを 16 パターンでカスタマイズできるようにした。
- Help メニューから Command 一覧を確認できるようにした。
- FD/WinFD互換ショートカットと一覧表示モード切り替えの導線を整理した。

### 画像ビューア
- 矩形選択コピーに対応し、選択範囲のみを `Ctrl+C` でコピーできるようにした。
- 右90度回転、左90度回転、左右反転、上下反転に対応した。
- 画像情報ダイアログを追加し、VideoStill 時は動画メタ情報も表示できるようにした。
- モデルレス表示中の前面化挙動と `Esc` による close 契約を改善した。

### クリップボード貼り付け
- 設定 ON 時のみ、テキストクリップボードを `.txt` ファイルとして貼り付けできるようにした。
- 画像貼り付け、テキスト貼り付け、ファイルコピー貼り付けで新規作成されたファイルを `Ctrl+Z` で取り消せるようにした。
- 上書き、統合、失敗、キャンセルを含む貼り付けでは Undo 記録しない安全仕様にした。

### 外部ツール連携・安全性
- 7-Zip、ffmpeg、ffprobe、ffplay、Explorer、PowerShell、外部エディタの起動境界を見直した。
- 外部ツール起動で、未信頼なパスやファイル名を shell 文字列として危険に連結しないよう補強した。
- 外部ツールパスの正規化、存在確認、実行前検証を追加した。
- `&`、`|`、`;`、空白、日本語を含むファイル名でも外部ツール起動が破綻しにくいよう改善した。

### 表示・設定・内部構造
- 初回セットアップを個別オプション方式へ整理した。
- 一覧表示モード（ファイル名のみ / サイズ / サイズ+更新日時）を追加し、サイズ形式設定に追従するよう改善した。
- 表示色プリセットとユーザー配色プリセットの導線を追加した。
- Logdsk / Move 先入力欄の履歴呼び出し（Up/Down）と補完（Tab）の責務を整理した。
- アンダーラインカーソル表示を追加した。
- CommandRegistry / CommandDispatcher を導入し、MainForm に集中していた Browser 入力処理と Command 実行境界を整理した。
- symlink / junction / reparse point を含む削除・移動で、リンク先実体へ再帰しない安全境界を追加した。
- reparse point directory のコピーと別ドライブ move fallback は、安全側でブロックする仕様にした。

## v2026.05.20

### 追加
- 初回起動時の「初回セットアップ」画面を追加。
  - 利用モードを選択できます。
  - FD互換ファンクションキー配置を選択できます。
  - 動画ファイルの Enter 動作を選択できます。
  - 7-Zip、動画ツールフォルダ、外部エディタを任意で設定できます。
- 設定画面の「起動 / 復元」から、初回セットアップを再表示できるようにしました。
- 動画ファイルの Enter 動作を外部再生に切り替える設定を追加しました。

### 変更
- 動画ツール設定を、`ffmpeg.exe` 個別指定から「動画ツールフォルダ」指定へ変更しました。
- `ffmpeg.exe`、`ffplay.exe`、`ffprobe.exe` を役割別に自動検出するようにしました。
- 動画の外部再生は、`ffplay.exe` が未設定の場合でも Windows の関連付けで開くようにしました。
- ffmpeg 未設定時の動画プレビュー導線は、黒い空画面ではなく Binary Viewer へフォールバックするようにしました。

### 修正
- 動画静止画プレビューが黒画面になることがある問題を修正しました。
- 初回セットアップ画面の文字あふれとボタン配置を調整しました。
- Browser 上での Ctrl+Enter による動画外部再生導線を整理しました。

### 注意
- 動画静止画プレビューには `ffmpeg.exe` が必要です。
- 動画の外部再生は、`ffplay.exe` がなくても Windows の関連付けで開けます。
- 初回セットアップの再表示は、全設定の初期化ではありません。

## v2026.05.17

### 初回公開
- MidFD の初回公開版です。
- キーボード中心のファイル操作、タブ、カテゴリ、マーク、基本プレビュー、7-Zip 連携、外部ツール連携などを含みます。
